<% if @year_in_review_release_flop.persisted? %>
   $('#release_<%= @year_in_review_release_flop.release_id %>').remove();
<% else %>
  alert("<%= raw I18n.t('year_in_review_music_release_flops.create.failed', errors: @year_in_review_release_flop.errors.full_messages.join('. '))%>");
<% end %>
