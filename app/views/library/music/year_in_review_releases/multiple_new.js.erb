<% if @releases.none? %>
  $('#years_in_review').html('<%= escape_javascript render(partial: 'library/music/year_in_review_releases/collection') %>');
  window.make_year_in_review_music_releases_sortable();
  $('#bootstrap_modal').modal('hide')
<% else %>
  $('#bootstrap_modal').html('<%= escape_javascript render(partial: 'library/music/year_in_review_releases/multiple_form') %>');
<% end %>